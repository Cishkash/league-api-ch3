import { noSpace } from 'league-app/helpers/no-space';
import { module, test } from 'qunit';

module('Unit | Helper | no space');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noSpace([42]);
  assert.ok(result);
});
