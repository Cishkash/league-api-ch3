<div class="media">
  <div class="media-left">
    <a href="#">
      <img src={{profileEndpoint}}/>
    </a>
  </div>

  <div class="media-body">
    <h2 class="media-heading">{{model.name}}</h2>
    <small>Summoner level {{model.summonerLevel}}</small>
  </div>
</div>

{{yield}}
